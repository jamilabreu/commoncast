.section
	.profile
		.profile-image
			= image_tag @user.image_url(:profile)
		.profile-main
			.profile-communities
				ul
					- @user.try(:communities).try(:map) do |community|
						li= link_to community.name, community_path(community)
